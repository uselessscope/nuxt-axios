export default useAxiosConfig({
  /**
   * Nuxt module configuration for Axios.
   *
   * This configuration allows you to set default options for Axios requests,
   * including the protocol (HTTP/HTTPS), host, port, and URL prefix.
   *
   * @type {NuxtAxiosConfigOptions}
   */

  defaults: {
    /** */
  },
})
